<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ru.nikitasemiklit.diploma">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.activities.TabBarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.fragments.LoginFragment"
            android:label="Вход">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity android:name=".ui.activities.ConferenceDetailActivity" />
        <activity android:name=".ui.activities.ReportRegistrationActivity" />
        <activity android:name=".ui.activities.SingupActivity" />
        <activity android:name=".ui.activities.TimeTableActivity" />
        <activity android:name=".ui.activities.SectionTableActivity" />
        <activity
            android:name="com.vk.sdk.VKServiceActivity"
            android:label="ServiceActivity"
            android:theme="@style/VK.Transparent" />
        <activity android:name=".ui.activities.CreateConferenceActivity" />
        <activity android:name=".ui.createConference.CreateConferenceActivity" />
        <activity android:name=".ui.activities.ConferenceListActivity" />
        <activity android:name=".ui.activities.ActivityReportConfirmation" />
    </application>

</manifest>